<%= render partial: "layouts/mailer/header" %>

      <h2 style="text-align: center; margin: 30px 0 15px; padding: 0; <%= mail_styles(:h2, :text) %>">
          It is time to Recognize
      </h2>


      <h3 style="text-align: center; margin: 0px 0 30px 0; padding: 0; <%= mail_styles(:h3, :text) %>">
        Your company has not made any recognitions. <%= link_to "Contact us", contact_url, style: mail_styles(:a) %> to learn best practices.
      </h3>

      <div style="text-align: center; margin: 10px 0 30px 0;">
        <%= link_to "Send public recognition to a colleague that does good work, but doesn't get enough credit" , recognitions_url(network: @user.network, trk: "valycr"), style: mail_styles(:button)%>
      </div>
            
<%= render partial: "layouts/mailer/footer" %>