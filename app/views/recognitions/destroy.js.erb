(function(){
  var bodyId = $body.attr("id"), card;

  if (bodyId === "recognitions-show") {
    Turbolinks.visit("/", { action: "replace" });
  } else {
    $(".isotope").isotope( 'remove', $("#recognition-card-<%= @recognition.id%>") );
  }
})();
